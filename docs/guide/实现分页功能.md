现在我对项目的全貌有了清晰的了解。让我帮你梳理前端分页功能的实现方案。

---

## 📊 当前项目分页现状分析

| 页面 | 数据加载方式 | 有无分页 |
|------|-------------|---------|
| **Home.vue** | 从 `allPosts` computed 取 `featuredPosts` / `recentPosts`（全量加载后筛选） | ✅ 无需（只取前5篇） |
| **Archives.vue** | 从 `allPosts` 做按年月分组（全量加载） | ❌ 目前无，但未来数据量大时需要 |
| **PostSearch.vue** | 通过 `getPosts` 传 `skip`/`limit` 分页拉取 | ⚠️ 后端参数对了，但前端 **没有页码导航 UI** |
| **Dex.vue** | 通过 `getDexEntries()` **无参数**调用，全量加载后客户端筛选 | ❌ 后端支持 `skip`/`limit`，但前端根本没传 |

**关键瓶颈：** 后端 `GET /posts` 和 `GET /dexs` 都只返回数据数组，**不返回总条数**。前端无法知道总页数，也就做不了标准的页码导航。

---

## 🛠 完整方案（两步走）

### 第一步：后端改造——让 API 返回分页元信息

当前后端返回是裸 `list[PostResponse]`，缺少分页必要字段：
```python
# 现状
@router.get("", response_model=list[PostResponse])  # 只有数据，没有页码

# 需要返回类似
{
    "items": [...],
    "total": 42,
    "skip": 0,
    "limit": 10
}
```

**方案：新建一个通用分页 Schema**

在 `server/app/schemas/post.py` 里追加（或用泛型）：

```python
# 分页包裹结构
from pydantic.generics import GenericModel
from typing import TypeVar, Generic

T = TypeVar("T")

class PaginatedResponse(GenericModel, Generic[T]):
    items: list[T]
    total: int
    skip: int
    limit: int
```

然后在 `routers/posts.py` 中改 `list_posts`：

```python
@router.get("", response_model=PaginatedResponse[PostResponse])
async def list_posts(
    published: bool | None = None,
    featured: bool | None = None,
    category: str | None = None,
    tag: str | None = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(10, ge=1, le=100),
    db: AsyncSession = Depends(get_db)
):
    items = await crud.get_posts(...)
    total = await crud.get_total_posts_count(db, published_only=published if published is not None else True)
    return PaginatedResponse(items=[PostResponse(**format_post(p)) for p in items], total=total, skip=skip, limit=limit)
```

对 `GET /dexs` 做同样改造。在 `crud/dexs.py` 加一个 `get_dex_count`。

---

### 第二步：前端改造——在需要分页的页面加 UI

#### A) 通用分页组件 `Pagination.vue`

新建 `client/src/components/ui/Pagination.vue`：

```vue
<script setup lang="ts">
const props = defineProps<{
  total: number
  skip: number
  limit: number
}>()

const emit = defineEmits<{
  (e: 'page-change', skip: number): void
}>()

const currentPage = computed(() => Math.floor(props.skip / props.limit) + 1)
const totalPages = computed(() => Math.ceil(props.total / props.limit))
const hasPrev = computed(() => currentPage.value > 1)
const hasNext = computed(() => currentPage.value < totalPages.value)
</script>

<template>
  <div v-if="totalPages > 1" class="flex items-center justify-center gap-2 mt-8">
    <button :disabled="!hasPrev"
      class="px-3 py-1 border-2 disabled:opacity-40"
      @click="emit('page-change', skip - limit)">‹ 上一页</button>

    <span class="px-3 py-1 text-sm">
      {{ currentPage }} / {{ totalPages }}
    </span>

    <button :disabled="!hasNext"
      class="px-3 py-1 border-2 disabled:opacity-40"
      @click="emit('page-change', skip + limit)">下一页 ›</button>
  </div>
</template>
```

#### B) 改造 API 层和前端的类型

**`client/src/types/post.ts`** 新增：

```typescript
export interface PaginatedResponse<T> {
  items: T[]
  total: number
  skip: number
  limit: number
}
```

**`client/src/api/index.ts`** 改造 posts.list 返回 `PaginatedResponse<PostResponse>`：

```typescript
list: (params?: {...}) => {
  const query = params ? '?' + ... : ''
  return request<PaginatedResponse<PostResponse>>(`/posts${query}`)
}
```

同样改造 `dexs.list`。

#### C) 改造 Store——保存 total 并返回分页数据

**`post.ts`** 改造 `getPosts`：

```typescript
const totalPosts = ref(0)

const getPosts = async (params?: {...}) => {
  loading.value = true
  try {
    const result = await api.posts.list(params)
    posts.value = result.items
    totalPosts.value = result.total
  } catch (err: any) {
    ...
  } finally {
    loading.value = false
  }
}
```

#### D) **改造 PostSearch.vue** —— 加上分页控件

当前 PostSearch.vue 已从路由 query 取 `page`、传 `skip/limit`，只是缺了 UI。在文章列表下方加上：

```vue
<Pagination
  v-if="totalPosts > limit"
  :total="totalPosts"
  :skip="(Number(route.query.page || 1) - 1) * 10"
  :limit="10"
  @page-change="(skip) => router.push({
    query: { ...route.query, page: Math.floor(skip / 10) + 1 }
  })"
/>
```

#### E) **改造 Dex.vue** —— 从全量客户端筛选改为服务端分页

当前 Dex.vue 把 `dexEntries` 全量拉到内存，然后在 `filteredEntries` 里做 `filter`。数据量大了以后应该改为：

1. 用 `skip`/`limit` 调用 `getDexEntries`，传入筛选参数
2. 将 `filteredEntries` 改为从 store 的当前分页数据 + 本地筛选
3. 加 Pagination 组件

或者更简单的过渡方案：先保持当前全量加载 + 客户端筛选，但加上 Pagination 对 `filteredEntries` 做**前端切片**：

```vue
<script setup lang="ts">
const pageSize = 20
const currentPage = ref(1)

const paginatedEntries = computed(() => {
  const start = (currentPage.value - 1) * pageSize
  return filteredEntries.value.slice(start, start + pageSize)
})

const totalPages = computed(() =>
  Math.ceil(filteredEntries.value.length / pageSize)
)
</script>

<template>
  <!-- 列表 -->
  ...
  <!-- 分页 -->
  <Pagination
    v-if="totalPages > 1"
    :total="filteredEntries.length"
    :skip="(currentPage - 1) * pageSize"
    :limit="pageSize"
    @page-change="(skip) => currentPage = Math.floor(skip / pageSize) + 1"
  />
</template>
```

> 等日后 Dex 数据量大了再切回**服务端分页**，那时只需把筛选参数传给 `getDexEntries` 即可。

---

### 📋 总结：需要改动的文件清单

| 改动 | 文件 | 说明 |
|------|------|------|
| **后端** | `server/app/schemas/post.py` | 新增 `PaginatedResponse` 泛型 Schema |
| | `server/app/routers/posts.py` | `list_posts` 返回分页包裹，查询总条数 |
| | `server/app/routers/dexs.py` | `list_dex` 同上改造 |
| | `server/app/crud/dexs.py` | 新增 `get_dex_count` 函数 |
| **前端** | `client/src/components/ui/Pagination.vue` | **新建**通用分页组件 |
| | `client/src/types/post.ts` | 新增 `PaginatedResponse<T>` 类型 |
| | `client/src/types/dex.ts` | 同上 |
| | `client/src/api/index.ts` | `posts.list` 和 `dexs.list` 返回类型改为包裹结构 |
| | `client/src/store/post.ts` | `getPosts` 接收分页响应并保存 `totalPosts` |
| | `client/src/store/dex.ts` | `getDexEntries` 同上改造 |
| | `client/src/views/PostSearch.vue` | 加 `<Pagination>` 组件 + 响应 page 变化 |
| | `client/src/views/Dex.vue` | 加前端切片分页或过渡到服务端分页 |
| | `client/src/views/Archives.vue` | 可选——数据量大时也可加 "每页N年" 分页 |

**优先级建议：** PostSearch 分页 UI → Dex 前端切片分页 → 后端返回 total 改造 → Pagination 组件复用。

需要我开始动手改哪些部分？还是你希望我针对某个特定页面再深入给更详细的代码示例？