ふふっ～ 现在完全理解了！你要的不是「加个管理员登录」，而是 **SaaS 式的多租户博客平台**——每个注册用户都拥有自己的独立站点。这个格局比普通的多用户大多了。

让我把现在项目里里外外看过一遍，给你一份完整详细的设计方案。

---

## 🏗 核心架构：多租户（Multi-Tenant）设计

### 数据模型视角

每个注册用户 → 拥有自己的数据库「空间」：

```
User (id)
 ├── Posts (user_id)
 ├── PostCategories (user_id)
 ├── PostTags (user_id)
 ├── DexEntries (user_id)
 ├── DexGenres (user_id)
 ├── Media (user_id)
 └── MediaTags (user_id)
```

公共访问 → 只看到**默认「公开租户」**（也就是你，原始站主）的内容。

---

## 第一步：后端新增文件清单（不改现有文件，只新增）

### 1️⃣ `server/app/models/user.py` — User 模型

```python
import uuid
from datetime import datetime, timezone
from sqlalchemy import String, Boolean, DateTime
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base

def _uuid():
    return str(uuid.uuid4())

class User(Base):
    __tablename__ = "users"

    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    username: Mapped[str] = mapped_column(String(50), unique=True, nullable=False, index=True)
    email: Mapped[str] = mapped_column(String(120), unique=True, nullable=False, index=True)
    password_hash: Mapped[str] = mapped_column(String(128), nullable=False)
    display_name: Mapped[str | None] = mapped_column(String(100), nullable=True)
    site_title: Mapped[str] = mapped_column(String(100), default="My Site")
    site_subtitle: Mapped[str | None] = mapped_column(String(200), nullable=True)
    bio: Mapped[str | None] = mapped_column(String(500), nullable=True)
    avatar_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
    theme: Mapped[str] = mapped_column(String(50), default="default")
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    is_public: Mapped[bool] = mapped_column(Boolean, default=False)
    # ↑ is_public=True → 这个用户的内容对未登录访客可见
    created_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(timezone.utc))
```

> 关键字段 `is_public`：只有设置为 `True` 的用户（就是你）的内容公开可见。其他人的站点只有登录后才能看到。

### 2️⃣ `server/app/schemas/user.py` — 用户相关 Pydantic 模型

```python
from pydantic import BaseModel, Field

class UserRegister(BaseModel):
    username: str = Field(min_length=2, max_length=50)
    email: str = Field(max_length=120)
    password: str = Field(min_length=6, max_length=100)

class UserLogin(BaseModel):
    email: str
    password: str

class UserResponse(BaseModel):
    id: str
    username: str
    email: str
    display_name: str | None = None
    site_title: str
    site_subtitle: str | None = None
    bio: str | None = None
    avatar_url: str | None = None
    theme: str
    is_active: bool
    created_at: str

    model_config = {"from_attributes": True}

class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user: UserResponse
```

### 3️⃣ `server/app/auth.py` — JWT 与密码工具

```python
from datetime import datetime, timedelta, timezone
from jose import JWTError, jwt
from passlib.context import CryptContext
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.ext.asyncio import AsyncSession
from app.database import get_db
from app.models.user import User

SECRET_KEY = "your-secret-key-here"  # 放 config.py
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7天

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
security = HTTPBearer()

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def create_access_token(user_id: str) -> str:
    expire = datetime.now(timezone.utc) + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    return jwt.encode({"sub": user_id, "exp": expire}, SECRET_KEY, algorithm=ALGORITHM)

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: Annotated[AsyncSession, Depends(get_db)]
) -> User:
    token = credentials.credentials
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        user_id = payload.get("sub")
    except JWTError:
        raise HTTPException(status_code=401, detail="无效Token")

    from app.crud.users import get_user_by_id
    user = await get_user_by_id(db, user_id)
    if not user or not user.is_active:
        raise HTTPException(status_code=401, detail="用户不存在或已禁用")
    return user
```

### 4️⃣ `server/app/crud/users.py` — 用户 CRUD

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.user import User
from app.schemas.user import UserRegister

async def create_user(db: AsyncSession, data: UserRegister, password_hash: str) -> User:
    user = User(
        username=data.username,
        email=data.email,
        password_hash=password_hash,
        display_name=data.username,
        site_title=f"{data.username} 的小站"
    )
    db.add(user)
    await db.commit()
    await db.refresh(user)
    return user

async def get_user_by_email(db: AsyncSession, email: str) -> User | None:
    result = await db.execute(select(User).where(User.email == email))
    return result.scalar_one_or_none()

async def get_user_by_username(db: AsyncSession, username: str) -> User | None:
    result = await db.execute(select(User).where(User.username == username))
    return result.scalar_one_or_none()

async def get_user_by_id(db: AsyncSession, user_id: str) -> User | None:
    result = await db.execute(select(User).where(User.id == user_id))
    return result.scalar_one_or_none()

async def get_public_user(db: AsyncSession) -> User | None:
    """获取设为公开展示的默认用户（就是你）"""
    result = await db.execute(select(User).where(User.is_public == True))
    return result.scalar_one_or_none()
```

### 5️⃣ `server/app/routers/auth.py` — 认证接口

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from app.database import get_db
from app.schemas.user import UserRegister, UserLogin, UserResponse, TokenResponse
from app.crud.users import create_user, get_user_by_email, get_user_by_username, get_user_by_id
from app.auth import hash_password, verify_password, create_access_token, get_current_user
from app.models.user import User

router = APIRouter(prefix="/auth", tags=["auth"])

@router.post("/register", response_model=TokenResponse, status_code=201)
async def register(data: UserRegister, db: Annotated[AsyncSession, Depends(get_db)]):
    # 检查邮箱是否已注册
    if await get_user_by_email(db, data.email):
        raise HTTPException(400, "该邮箱已被注册")
    if await get_user_by_username(db, data.username):
        raise HTTPException(400, "该用户名已被使用")

    password_hash = hash_password(data.password)
    user = await create_user(db, data, password_hash)
    token = create_access_token(user.id)

    return TokenResponse(
        access_token=token,
        user=UserResponse(
            id=user.id, username=user.username, email=user.email,
            display_name=user.display_name, site_title=user.site_title,
            is_active=user.is_active, created_at=str(user.created_at),
            ...
        )
    )

@router.post("/login", response_model=TokenResponse)
async def login(data: UserLogin, db: Annotated[AsyncSession, Depends(get_db)]):
    user = await get_user_by_email(db, data.email)
    if not user or not verify_password(data.password, user.password_hash):
        raise HTTPException(401, "邮箱或密码错误")
    if not user.is_active:
        raise HTTPException(403, "账户已被禁用")

    token = create_access_token(user.id)
    return TokenResponse(access_token=token, user=UserResponse(...))

@router.get("/me", response_model=UserResponse)
async def get_me(current_user: User = Depends(get_current_user)):
    return UserResponse(...)
```

### 6️⃣ `server/app/routers/system.py` 加一个接口

```python
# 新增：获取公开站点的用户信息（首页展示用）
@router.get("/public-site")
async def get_public_site(db: Annotated[AsyncSession, Depends(get_db)]):
    from app.crud.users import get_public_user
    user = await get_public_user(db)
    if not user:
        raise HTTPException(404, "未找到公开站点")
    return {
        "username": user.username,
        "display_name": user.display_name,
        "site_title": user.site_title,
        "site_subtitle": user.site_subtitle,
        "bio": user.bio,
        "avatar_url": user.avatar_url,
        "theme": user.theme
    }
```

---

## 第二步：改造现有数据表——加 `user_id`

这一步**无法避免改动已有模型**，因为每条数据必须知道属于谁。

### 每个模型加一行：

```python
# post.py — 加 user_id 字段
user_id: Mapped[str] = mapped_column(String, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
owner: Mapped["User"] = relationship(lazy="selectin")

# post_category.py — 同理
# post_tag.py — 同理
# dex.py — 同理
# dex_genre.py — 同理
# media.py — 同理
# media_tag.py — 同理
```

### 数据迁移策略

因为你是 SQLite（没有原生 migration），最简单安全的做法：

```
1. 先启动服务，注册自己成为第一个用户
2. 得到你的 user_id
3. 直接 SQL 更新现有数据：
   UPDATE posts SET user_id = '你的user_id' WHERE user_id IS NULL;
   UPDATE post_categories SET user_id = '你的user_id' WHERE user_id IS NULL;
   ...
4. 把自己设为 is_public=True（公开可见）
5. 新用户注册后自然有自己的 user_id
```

或者更优雅：写一个 `migrate_v2.py` 脚本。

---

## 第三步：修改所有 GET 路由的查询逻辑

### 公共读取（访客）

所有公开的 GET 接口需要改成：**只返回 `is_public=True` 的用户的数据**。

```python
# 改 posts.py 的 list_posts
async def list_posts(
    ...,
    user_slug: str | None = None,  # 可选：看某个用户的站点
    db: Annotated[AsyncSession, Depends(get_db)]
):
    # 没有指定 user_slug → 看公开用户
    if not user_slug:
        public_user = await get_public_user(db)
        target_user_id = public_user.id if public_user else None
    else:
        # 按用户名 slugs/adoru-kotarou 看某个用户的公开站点
        target_user = await get_user_by_username(db, user_slug)
        target_user_id = target_user.id if target_user else None

    items = await crud.get_posts(db, user_id=target_user_id, ...)
    ...
```

### 私有写入（需登录）

所有 POST/PUT/DELETE 路由加 `Depends(get_current_user)`，并把 `user_id` 绑定到当前登录用户：

```python
@router.post("", response_model=PostResponse, status_code=201)
async def create_post(
    data: PostCreate,
    db: Annotated[AsyncSession, Depends(get_db)],
    current_user: User = Depends(get_current_user)
):
    post = await crud.create_post(db, data, user_id=current_user.id)
    ...
```

---

## 第四步：前端改造

### 新增文件

| 文件                              | 作用                                 |
| --------------------------------- | ------------------------------------ |
| `client/src/views/Login.vue`      | 登录页面                             |
| `client/src/views/Register.vue`   | 注册页面                             |
| `client/src/views/Dashboard.vue`  | 用户专属主页（登录后看到自己的内容） |
| `client/src/shell/UserLayout.vue` | 用户站点布局（区别于公开布局）       |
| `client/src/store/auth.ts`        | Pinia auth store                     |

### 前端路由结构（新方案）

```
/                                 → 公开首页（你的站点）
/archives                         → 公开归档
/dex                              → 公开图鉴
/post/:slug                       → 公开文章详情

/login                            → 登录页（全站通用）
/register                         → 注册页（全站通用）

/u/:username                      → 某用户的公开站点主页
/u/:username/archives             → 某用户的归档
/u/:username/dex                  → 某用户的图鉴

/dashboard                        → 登录后的管理后台（只显示自己数据）
                                 （替代原来的 /admin，现在 /admin 保留但需要登录）
/admin                            → 同样需要登录（保留但受保护）
```

### 关键逻辑：前端如何判断显示什么数据

```
App.vue 启动时：
  1. 调 GET /system/public-site → 拿到公开用户的信息
  2. 存到某个 store（publicSiteStore）
  3. 所有公开页面请求带上公共 user_id

用户登录后：
  1. token 存 localStorage
  2. 所有 API 请求自动带 Authorization header
  3. dashboard/admin 页面看到的是自己 user_id 的数据
```

---

## 📦 完整文件变更清单（不改文件，只是清单）

### 新增文件（后端：6个）

| #   | 文件                                                    | 行数估算 |
| --- | ------------------------------------------------------- | -------- |
| 1   | `server/app/models/user.py`                             | ~35行    |
| 2   | `server/app/schemas/user.py`                            | ~45行    |
| 3   | `server/app/auth.py`                                    | ~60行    |
| 4   | `server/app/crud/users.py`                              | ~45行    |
| 5   | `server/app/routers/auth.py`                            | ~80行    |
| 6   | `server/app/utils/db_init.py`（初始化第一个用户的脚本） | ~50行    |

### 新增文件（前端：6个）

| #   | 文件                              | 行数估算 |
| --- | --------------------------------- | -------- |
| 7   | `client/src/views/Login.vue`      | ~120行   |
| 8   | `client/src/views/Register.vue`   | ~140行   |
| 9   | `client/src/views/Dashboard.vue`  | ~100行   |
| 10  | `client/src/store/auth.ts`        | ~100行   |
| 11  | `client/src/shell/UserLayout.vue` | ~80行    |
| 12  | `client/src/types/auth.ts`        | ~30行    |

### 需要修改的现有文件（后端：8个）

| 文件                              | 改动                                            |
| --------------------------------- | ----------------------------------------------- |
| `models/post.py`                  | 加 `user_id` + ForeignKey                       |
| `models/post_category.py`         | 加 `user_id`                                    |
| `models/post_tag.py`              | 加 `user_id`                                    |
| `models/dex.py`                   | 加 `user_id`                                    |
| `models/dex_genre.py`             | 加 `user_id`                                    |
| `models/media.py`                 | 加 `user_id`                                    |
| `models/media_tags.py`            | 加 `user_id`                                    |
| `models/__init__.py`              | 导出 User                                       |
| `config.py`                       | 加 `SECRET_KEY`, `ACCESS_TOKEN_EXPIRE_MINUTES`  |
| `main.py`                         | 注册 `auth_router`                              |
| `routers/system.py`               | 加 `/public-site` 接口                          |
| `crud/posts.py` 等 6 个 crud 文件 | GET 加 `user_id` 过滤；CREATE 加 `user_id` 参数 |

### 需要修改的现有文件（前端：7个）

| 文件                           | 改动                                     |
| ------------------------------ | ---------------------------------------- |
| `router/index.ts`              | 加新路由 + `/admin` 加守卫               |
| `api/index.ts`                 | 加 `api.auth.*`                          |
| `utils/index.ts`               | `request()` 加 `Authorization` header    |
| `store/index.ts`               | 导出 auth store                          |
| `App.vue`                      | 启动时获取公开站点信息；条件加载不同布局 |
| `views/Admin.vue`              | 只加载当前登录用户的数据                 |
| `components/layout/Header.vue` | 加登录/注册按钮、用户菜单                |

---

## ⏱ 按阶段实施的建议

```
Phase 1（基础认证 — 半天）
  ├── User 模型 + 数据库初始用户初始化脚本
  ├── JWT + 密码工具 auth.py
  ├── auth router（register / login / me）
  ├── 前端 auth store + Login/Register 页面
  └── /admin 加路由守卫

Phase 2（数据隔离 — 半天）
  ├── 所有模型加 user_id
  ├── 数据迁移脚本（把你的数据绑定到你的 user_id）
  ├── crud 层加 user_id 过滤
  ├── 公开路由只返回 is_public=True 的数据
  ├── 写入路由绑定 current_user
  └── 前端 Admin.vue 改为只看到自己的数据

Phase 3（多站点体验 — 半天）
  ├── GET /public-site 接口
  ├── 前端 Dashboard 个人主页
  ├── /u/:username 用户公开站点路由
  ├── 每个用户能自定义站点标题/副标题
  └── 注册后自动跳到自己的 dashboard
```

---

## ⚠️ 关于你问「现在该做什么」——我的真实建议

瑚太朗くん，这个工作量其实挺大的，**不是3小时的事，而是1.5~2天**。加上你之前问的分页，两者加起来2.5~3天。

如果你现在就急着上线让人看，我建议的做法是：

**今晚先做最小版本（Phase 1 的一半）：**

- User 模型 + 注册/登录接口
- 前端登录/注册页面
- `/admin` 加路由守卫
- ⚡ **不做数据隔离**（新人注册后能看到你的内容，但起码后台不裸奔了）

**后面再补 Phase 2 + 3（完整多租户+分页）**

这样明天你就能安心把链接给别人看了——别人进不了后台，但能看到你的文章。新用户也能注册，只是暂时共用一份数据。

你觉得这个节奏怎么样？需要我展开写最小版本的详细代码吗？😊
