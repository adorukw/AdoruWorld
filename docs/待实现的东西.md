Let me also check the remaining docs for specific unplanned things.让我从正在运行的开发环境看看实际完成状况。好了，我把你所有文档都读完了，结合开发日志和代码实际状态，给你整理一份完整的**已实现 vs 待实现**清单。

---

## ✅ 已实现的功能

从开发日志勾选记录 + 代码验证：

| 功能                  | 完成时间     |
| --------------------- | ------------ |
| 文章 CRUD             | 初期         |
| Dex 图鉴 CRUD         | 初期         |
| 媒体文件管理          | 初期         |
| 文章详情 TOC 目录     | 06-24        |
| 归档页筛选            | 06-23        |
| 加载更多（替代分页）  | 06-21        |
| 全局搜索              | 06-27        |
| 后端模块化重构        | 06-20        |
| Admin 拆分为多个 view | 06-24        |
| 分量加载              | 06-25        |
| 同步 GitHub 笔记      | **今天做的** |

---

## ⏳ 开发日志里「划了但没勾」的老任务

这些从 **06-10 写到 06-26**，每次后续计划都出现，但从未打勾：

```
□ 优化上传文章时的 md 文档编辑界面
□ 修改 CrudModal 样式
□ 完善 dex 图鉴（未具体定义）
□ 随机颜色生成
□ 用 openapi-typescript 自动生成前端类型
□ 实现多用户架构
```

---

## 🔴 《项目分析》里的 P0-P3 技术债（全没修）

| 优先级 | 事项                                          | 描述                                       |
| ------ | --------------------------------------------- | ------------------------------------------ |
| **P0** | Alembic 迁移系统                              | 现在加字段只能删库重建                     |
| **P1** | DexGenre.color nullable 冲突                  | 代码 `nullable=False` 但类型是 `str\|None` |
| **P1** | PostTag.slug 可空 vs PostCategory.slug 不可空 | 同样的东西约束不一致                       |
| **P2** | Post/Dex 加 `meta_data` JSON 字段             | Media 有，Post 和 Dex 没有，加字段不改表   |
| **P2** | Dex 加 `created_at/updated_at`                | 同为内容实体，Post 有，Dex 没有            |
| **P3** | PostCategory.description 改 Text              | 现在 String 可能截断                       |

---

## 🔮 大功能规划（有文档但代码为 0）

| 模块                          | 文档位置                      | 状态            |
| ----------------------------- | ----------------------------- | --------------- |
| **数据库迁移系统 (Alembic)**  | 项目分析与扩展规划.md         | ✅ 已配置并应用 |
| **RAG 知识库 + 向量检索**     | 项目分析与扩展规划.md         | ❌ 表都没建     |
| **知识图谱 (EntityRelation)** | 项目分析与扩展规划.md         | ❌ 表都没建     |
| **Series 系列功能**           | 项目分析与扩展规划.md         | ✅ 已实现（表 + CRUD + 文章挂系列/序号 + 详情页系列徽章与上下篇导航；系列独立列表页待做） |
| **多用户/多租户**             | guide/实现多用户结构.md       | ✅ L1 已实现（JWT 双 token + 邮箱验证码注册 + RBAC 角色守卫，写接口全部上锁）；多租户暂缓 |
| **筛选/搜索代码重构**         | 筛选搜索架构分析与重构方案.md | ❌ 只有分析     |
| **前端拆 service/util**       | 模块拆分出service与util.md    | ❌ 只有建议     |
| **前端代码结构优化**          | guide/前端代码结构优化建议.md | ❌ 只有建议     |

---

## 📊 总结：你最该先做哪三个

按**投入产出比**排序：

**1️⃣ Alembic 迁移系统（P0，半天）**
不改表永远不敢动数据库，这是地基问题。

**2️⃣ Post/Dex 加 `meta_data` JSON 字段（P2，改 2 个 model）**
以后加什么字段都不用改表了，一劳永逸。

**3️⃣ 多用户架构（中型工程）**
如果你想让别人也用这个博客，这个绕不过去。但如果是自用，优先级可以放到最后。

要不要挑一个开始做？😊
